---
alwaysApply: true
---

# Session Objective Protocol

## 触发条件

发现阶段（扫描已有文件）始终执行，不受触发条件限制。触发条件仅控制是否**新建** objective。

**硬触发**（无需判断，不适用"不确定时先执行"的宽限）：
- 用户明确要求跟踪进度（如"帮我维护目标"、"记录进度"、"建个 objective"等）→ 进入创建流程
- 用户要求记住某条要求或约束（如"记住这个"、"以后都要这样"、"这个很重要别忘了"等）→ 若已有 active objective，立即追加到关键约束列表；若无 active objective，先创建再追加

**条件触发**（需判断后决定）：
- 任务涉及**两个及以上可识别的执行步骤**（每个步骤有独立的、可验证的产出）

**怎么判断"两个及以上执行步骤"**：
- "改 schema → 改 API → 改前端"——三个步骤，每个有独立产出，应创建
- "写一个函数并加测试"——虽然是两件事，但产出紧密耦合、通常在一轮完成，不需要创建
- "查一个文件然后回答问题"——本质是单步查询，不需要创建

单步骤操作、纯问答、快速查询直接执行，不创建 objective。如果开始时无法判断步骤数，先执行第一步；**完成第一步后必须重新评估**：若剩余任务仍有两步及以上，立即暂停并创建 objective，再继续执行。

## 发现与绑定

每次会话开始时，扫描 `.ai-objectives/` 目录下所有 `.md` 文件。如果目录不存在，跳过发现阶段，按触发条件决定是否创建。**执行过程中识别到任务步骤超出预期时，同样触发此扫描与绑定流程。**

1. **存在 `status: active` 的文件时**（无论当前任务是否简单，都应通知用户，避免遗忘进行中的工作）：
   - 读取每个文件的 frontmatter `objective` 和 `created` 字段
   - 如果只有一个 active 文件：向用户确认："发现一个进行中的目标：`<objective>`（创建于 `<created>`，文件：`<文件名>`），是否继续？"
   - 如果有多个 active 文件：列出所有文件的**文件名 + objective + created 时间**，让用户选择
   - 如果文件创建时间超过 7 天，额外提示可能已过时，询问是否归档
   - 用户确认后绑定该文件；用户否认后，询问是要新建、归档/清除、还是**暂时忽略**（不绑定任何 objective，按正常模式工作）
2. **不存在 active 文件时**：如果当前任务满足触发条件，进入创建流程

**绑定后不得切换**：如果用户对话方向明显偏离当前 objective，应提示用户而非静默切换。

<!-- probe-only:start -->
## 协议详情

触发创建流程，或发现 active objective 文件需要绑定时，**必须先读取** `.cursor/references/conductor-protocol.md`，再执行后续步骤。该文件包含完整的创建格式、更新规则、步骤间检查和执行约束。
<!-- probe-only:end -->
